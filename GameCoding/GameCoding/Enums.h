#pragma once

enum class SceneType
{
	None,
	DevScene,
	//GameScene,
	EditScene,
	//MenuScene,
	//FortressScene,
};

enum class PlayerType
{
	CannonTank,
	MissileTank,
};

enum class Dir
{
	Left,
	Right,
};